// TODO: remove player by id/order number
// Might have to separate id by order number
// where order number will just be for the order in the list
// and id will represent the client handle
/* 
 for(int i = oldId; i < players.Count; i++)
 {
   players[i].id = i+1;
 }
 UpdatePlayerNameList();
*/
